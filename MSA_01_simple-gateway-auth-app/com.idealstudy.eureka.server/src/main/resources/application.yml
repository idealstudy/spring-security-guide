server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false  # ?? Eureka ??? ? ??? ???? ??
    fetch-registry: false  # ?? Eureka ??? ?????? ???? ??
  server:
    enable-self-preservation: false  # ?? ?? ?? ????

spring:
  application:
    name: eureka-server